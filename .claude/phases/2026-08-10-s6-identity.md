# S6 — Identity: armour/combat stats + lobby ship-pick UI + audio + art/juice

**Slice goal:** the tech-demo → **SLUR** transition. The loop works (S1–S5); S6 is the pass
that makes it *feel like the game*. Four parallel identity/feel tracks.

Arc doc. Phases append below. Code lands in **Implement**, not before.

---

## Ideate  (2026-08-10)

### The framing: four gaps between "works" and "feels like SLUR"

| # | Gap today | Part | Character | Size |
|---|-----------|------|-----------|------|
| 1 | Ships **move** different (S3 flight tuning) but **fight** identical — same stun, same bolt | armour/`powerAffinity` combat stats | gameplay data + hit math | small |
| 2 | Choice is **invisible** — dev hot-swap keys 1–5, no commitment moment | real lobby ship-pick UI | UI (preview exists from S4) | small–med |
| 3 | It's **silent** — zero audio in the codebase | audio pass | **new subsystem** | large |
| 4 | It's **programmer-neon** — ADD aesthetic unredeemed | art/juice pass | polish (bounded by checklist) | bounded |

### Decisions locked this phase

- **Scope = full S6** (all four parts) — user chose, over my split-it recommendation. Accepted
  **on condition part 4 has a finite checklist** so "S6 done" stays answerable.
- **Center of gravity = all equal weight.** Sequence by *dependency only*, not felt-impact.
  Dependency map: **1 → 2** (pick screen renders part-1 stats); **3 and 4 fully parallel**.
- **Armour = sidegrade ONLY.** `armour` is the *readable name* for a tradeoff the flight tuning
  already imposes (big ship = tanky **and** sluggish), NOT a free stat. No "tank strictly better"
  axis. Upholds [[balance-at-playstyle-not-geometry]]: fairness = every ship has a viable
  win-con, not stat-equality.

### Design bets to validate in Brainstorm (not yet locked)

- **Audio should be SYNTHESIZED, not sampled** (SFX at least). A filtered Web Audio oscillator
  whose pitch tracks speed is *more* SLUR than a looped mp3 — fits TRON, dodges asset-licensing,
  matches the "deterministic, no baked geometry" ethos. Music may still be a track. → research
  Web Audio vs Tone.js vs sampled; positional model.
- **Audio architecture is a NON-NEGOTIABLE, not a detail.** Audio graph = long-lived resource →
  module singleton **outside React** (same rule that cost S2: no lifetime coupled to a
  `useEffect` cleanup). Positional panning driven off ECS in `useFrame`, never React state.
- **Part-4 checklist (starting bid, to finalize in Prep):** TRON **derezz-death** + ship **trail**
  + **LCARS HUD frame**. Everything else (comfort sliders, extra shaders) is explicitly OUT of S6.

### Success = human gate

Two-tab playtest where: you **pick** a ship on a real screen (not dev keys), the pick **changes
how it fights** (armour/affinity readable in play), you can **hear** engine/hit/pickup, and death
**derezzes**. If all four land and the balance philosophy still holds → S6 done.

### Open questions carried into Brainstorm

- Q1: `armour` — how does it enter the hit math? (stun-duration multiplier? hit-to-stun threshold?
  bolt knockback resist?) Must stay sidegrade.
- Q2: `powerAffinity` — in scope for S6, or defer? (adds a 2nd combat-identity axis; risk of
  over-scoping part 1.)
- Q3: Audio — synthesized vs sampled vs hybrid; library (raw Web Audio / Tone.js); asset sourcing
  for music; positional model (PannerNode vs manual stereo pan off ECS x-offset).
- Q4: Pick UI — does it replace dev keys 1–5 or coexist? lock-at-GO semantics (Race locks field)?
- Q5: Part-4 — derezz as shader (dissolve) vs particle-burst; trail as drei `<Trail>` vs instanced.

---

## Brainstorm  (2026-08-10)

Grounded every question in the actual code first (combat/constants, ship-classes, schema,
lobby-overlay, explosions). Two "open questions" partly dissolved on contact with the code.

### Decisions locked

- **Q1 · armour = per-class stun-duration multiplier.** New `stunResist` (or `armour`) field in
  `ship-classes.ts` FlightTuning (or a sibling combat block); server + client both resolve it →
  `effectiveStun = STUN_SECONDS * (1 - armour)` applied where `stunTimer` is set. **Stun is a
  predicted SimShip field**, so armour MUST live in the shared registry (byte-identical both ends)
  or prediction desyncs — same discipline as flight tuning. Sidegrade axis: **agile = fragile
  (long stun), heavy = tanky (short stun)**; agility ⊥ armour, no free stat. Upholds
  [[balance-at-playstyle-not-geometry]].
- **Q2 · `powerAffinity` DEFERRED.** Roster is one item (Bolt); affinity is meaningless until
  Mine/Shield/Boost exist. Add it with the 2nd pickup, not now. Part-1 = armour only.
- **Q3 · Audio = the AUDIO-doc approach.** three.js `PositionalAudio` (Web Audio), object-pooled,
  client-local, **CC-sourced samples**. Split: **engine hum = synthesized oscillator** (continuous
  pitch∝speed — the one cue a sample loop can't do cleanly); **all discrete SFX = CC sample files**;
  music = a CC synthwave track. No Howler for v1 (revisit only if bus/ducking needs it).
  - **ARCHITECTURE (non-negotiable):** AudioContext + master/duck buses = **module singleton
    OUTSIDE React**, resumed on first user gesture (host Start / player Join click). Per-ship
    `<PositionalAudio>` MAY be R3F (lifetime == ship entity). Never couple the context to a
    `useEffect` cleanup (the S2 bug). Bind SFX to the S5 events already broadcast (`usePowerUp`
    fire, `'hit'`, `stunTimer` edge, pickup grab, threat-HUD → incoming blip).
  - **Mix/duck order** (from doc §5): threat > own combat/impact > pickups/UI > engine > music.
    Master limiter + a fast mute/volume control (office).
- **Q4 · Pick UI mostly already exists.** `lobby-overlay.tsx` already picks ship+colour
  (`SET_CLASS_MESSAGE`), locks at GO, and shows the hero-orbit. Part-2 = **upgrade the picker to
  show IDENTITY**: class name + armour/agility profile + a real card (not a lowercase chip). Dev
  keys 1–5 stay as in-game hot-swap. Much smaller than backlogged.
- **Q5 · derezz = dissolve shader + keep the burst.** Noise-threshold clip on the ship model
  (authentic TRON dissolve) as the death; existing `explosions.tsx` shard-burst stays as the
  shatter accent. Trail = drei `<Trail>` for v1 (revisit perf at 8–12 ships against r3f.md).

### Loose thread carried into Prep

- **Audio asset sourcing** (the cost of choosing sampled SFX): need a **CC-licensed SFX pack**
  (laser/impact/pickup/UI chirps/countdown/respawn) + a **CC synthwave track** (in-run + calmer
  lobby idle) + a **credits list**. This is a real research/licensing task — decide in Prep whether
  to hunt sources now (sub-agent) or fold into Implement. Engine hum is synth → no asset needed.

### Audio asset sourcing — RESOLVED (license-verified, 2026-08-10)

Sub-agent surveyed Kenney / OpenGameArt / Freesound / incompetech, license read from each
primary page. **The whole SFX layer can be 100% CC0 → zero attribution.**

- **SFX backbone = Kenney (CC0, OGG-native):** Sci-Fi Sounds (laser), Interface Sounds (UI chirp
  family + countdown blips), Digital Audio (glitch/derezz), Impact Sounds (hit body / GO downbeat).
  One author ⇒ the UI family already sounds related. Hero-cue sharpeners from **Freesound CC0**:
  Kinoton laser (351429), PeteBarry stun (459800), JustInvoke pickup (138491), clif_creates
  warp/respawn (747184), gis_sweden threat-alarm (709403).
- **Music:** in-run = **Kevin MacLeod "Neon Laser Horizon"** (CC-BY, MP3+WAV — strongest driving
  synthwave) → **costs exactly ONE credit line**. Lobby idle = **The Cynic Project "Calm Ambient
  1/2"** (CC0, MP3). **RESOLVED 2026-08-10 — user accepts CC-BY:** MacLeod in-run + Cynic CC0 lobby;
  one credit line in CREDITS.md at Implement.
- **Format:** mp3/ogg/wav all decode via `decodeAudioData`; **aiff does NOT** — the 2 aiff picks
  (cool-buzz derezz, Warp! respawn) need transcode or use the OGG alternatives.
- **Gaps needing a custom edit (not off-the-shelf):** (a) **derezz** — CC0 "digital
  disintegration" one-shots are scarce; layer cool-buzz + a Kenney glitch. (b) **countdown→GO** —
  CC0 options are generic 3-2-1 beeps, not a *pitched* rising resolve; build from 3 ascending
  Kenney blips + an Impact "GO" hit.
- **Sonniss GDC bundle** noted but EXCLUDED (royalty-free, not CC — no-redistribute / no-AI-train).
- Credits block for the CC-BY items is drafted in the sub-agent output; fold into a CREDITS.md at
  Implement.

### Scope after Brainstorm (what actually ships)

1. **armour** — `ship-classes.ts` stat + wire into stun application (shared, predicted). *(small)*
2. **Pick UI upgrade** — identity/stats card on the existing lobby picker. *(small)*
3. **Audio** — singleton audio module + synth engine + sampled SFX bound to S5 events +
   positional + music + mute/volume. *(large — the real weight of S6)*
4. **Art/juice** — dissolve-shader derezz + drei `<Trail>` + LCARS HUD frame. **Checklist-bounded;
   everything else (comfort sliders, extra shaders) is OUT.** *(bounded)*
5. **Environment/scene pass** (ADD §11 — user pulled it into S6, 2026-08-10): unify solo+net scene,
   push bloom, atmosphere (`scene/environment.tsx`: fog + drei `<Stars>` + gradient backdrop),
   hull-colour, `COLOR_COUNT` 8→12. **Roughly doubles part-4 — checklist-bound it too.** *(medium)*

---

## Implement / As-built  (2026-08-10)

Three parallel **worktree** agents (audio · landing · procgen) built concurrently, merged incrementally into
`dev` with verify between each. Final tree GREEN: **51 shared tests · typecheck · lint · client build**.

**Built + committed:**
- **Landing UI** (`15229e8`) — de-rounded neon front-of-house over the Grid-Void scene; cyan×marigold;
  chamfered glass (clip-path + drop-shadow glow); matchmaking behaviour preserved; reusable LCARS-neon system
  in `app.css`. Files: `routes/home.tsx`, `routes/home/{landing-scene,landing-rig}.tsx`, `lobby/room-list.tsx`.
- **Procgen v2** (`b54b837`) — carved racing-line + variable-width noise-walls (`sim/noise.ts` + rewritten
  `sim/track.ts`); slope+curvature caps derived from the least-capable class; per-slice fairness. 40→51 tests.
- **Audio** (`0471d02`) — singleton engine OUTSIDE React (resume via global gesture), synth engine hum,
  15 Kenney CC0 SFX + CC-BY MacLeod / Cynic CC0 music, bound to S5 events + positional remote hum. `M`=mute.
  Files: `app/audio/**`, `public/audio/**`, 2-line mount in `net-canvas.tsx`.
- **Integration fixes** (`b827f54`) — pickups now emit in the racing-line corridor of every non-gap segment
  (`corridorCenterX`; density ~6→~60/track, ON the line); track.ts complexity refactor; audio `readonly` type.
- **LAN config** (`93ec151`) — server binds `0.0.0.0` + logs join URL; Vite `host:true` (the real blocker).
- **Removed** `/solo`; **added** `/env-lab` prototype (`52f5a04`).

**Deviations from plan (flagged for the feel-gate):**
- Walls are FULL-segment-depth (width variety ✓, depth ✗) — forced by client `BLOCK_LIMIT=128`. Depth variety
  later by raising it.
- `RemoteEngineAudio` (positional remote hum) built but NOT hear-verified — one removable line if it misbehaves.
- Procgen agent brief omitted `pnpm lint` → complexity violations slipped, fixed at merge. [[require-lint-in-agent-briefs]]

**REMAINING in S6 (not built this round):** `armour` (stun-multiplier sidegrade) · dissolve-shader derezz ·
pick-UI identity stats (audio left UI cues wired-but-uncalled) · integrate `environment.tsx` into the **in-game
net canvas** (only on landing/env-lab so far) · hull-colour + `COLOR_COUNT` 8→12 · ship trails.

**GATE PENDING:** human feel-gate — none of procgen/audio/landing has been *played* yet. Playtest before more build.

### Environment pass — decisions (2026-08-10)

- **World look = HYBRID ribbon + distant tube-walls** (ADD OQ2). Open ribbon underfoot (existing
  track, ZERO gameplay change) + parallax neon "canyon walls" FAR to each side (well beyond
  `HALF_WIDTH=32`, non-collidable, instanced+recycled like `scenery.tsx`) for enclosure feel.
- **Post-FX = CONSERVATIVE** (ADD OQ4): unify scene + push **bloom** (~1.0–1.5 / threshold ~0.4) +
  **starfield** + **fog** + **gradient backdrop** only. **DEFER** chromatic-aberration / scanline /
  vignette / grain (add after a 12-ship legibility+perf test). SelectiveBloom if bolts/ships wash.
- **Palette (locked 2026-08-10):** signature duo = **cyan + marigold-amber ("gainda", `#00e5ff` ×
  `#ff9f1c`)**, replacing cyan+magenta; magenta demotes to one of the ~12 player hues. Default 2-player
  contrast = cyan (local) / marigold (remote). **Game-code follow-up (Implement):** swap `scene/ship.tsx`
  + `explosions.tsx` CYAN/MAGENTA + `colors.ts`; raise `COLOR_COUNT` 8→12 with value-staggering /
  max-pairwise auto-assign (ADD OQ3).
- **Chosen environment variant = `Grid Void`** (dense stars + cyan haze + cyan horizon-glow dome + mid
  cyan walls, leaning on the Track grid as ground) — matches the cyan-forward duo. Integrate
  `scene/environment.tsx` (Grid Void config) into the net canvas during Implement; retune wall
  density/height + bloom at the 12-ship legibility gate. Tube-walls may go more angular/greebled later
  (de-rounded direction).
- **Handoff = PROTOTYPE-FIRST.** Background agent produces isolated, parameterized environment
  VARIANTS as throwaway prototype code (a dev `/env-lab` route, additive — does NOT modify
  net/solo canvases yet); **main agent renders + screenshots each → user picks → then integrate.**
  Agent can't see the scene, so its output = research + param sets + wiring, not final beauty.

### Visual-polish pass + landing hero — as-built (2026-08-10, post feel-gate screenshot)

Reconciled a batch of art/bug fixes filed as GitHub issues #1–#5 (the issue tracker is now the task log;
this note is the narrative). All client-render only — no sim/shared change; gate GREEN (typecheck · lint · build).

- **Derezz dissolve now shows on death** (#4, commit `cf397ab`). `localDeathVfxSystem`/`remoteInterpSystem`
  hard-hid the whole ship group the instant `dead` flipped (leftover S5), so the S6 dissolve shader never
  rendered. Fix: the dissolve shader (`ship-model.tsx`) OWNS the death visual — keep the spectator hide + live
  stun strobe, but no longer `visible=false` on `dead`; respawn dissolves back in. Temp `console.warn`
  diagnostics that chased this bug were stripped from `ship-model.tsx` before commit. [[ship-feedback-with-the-mechanic]]
- **Track floor reads near-black** (#1). `track-view.tsx` floor emissive `0.6→0.05`, colour `#0d0d10→#050507` —
  the gray-white sheen washed to mid-gray under bloom; now deep void, all neon on the edge-rails.
- **Starfield reads in-game** (#3). `env-config.ts` Grid-Void stars pulled IN (radius `280→190`, less fogged),
  denser + bigger (`count 6000→8000`, `factor 4.5→6.5`) so they survive the cyan haze.
- **No stray white cube at the origin** (#2). `explosions.tsx` + `hit-spark.tsx` now park the instanced pool at
  MOUNT via a callback ref (fires during commit, before first paint) instead of in the first `useFrame` — which
  left MAX identity-matrix unit cubes at the origin for one frame. Not a mount effect — imperative init, no teardown.
- **Landing hero ship + TRON grid** (#5). New `routes/home/landing-ship.tsx` (cosmetic-only Quaternius
  `challenger`, bob+bank, rides the LandingRig drift entity — no ECS death/net/collision) + drei `<Grid>`
  infinite floor + a key `directionalLight` in `landing-scene.tsx`. The menu now reads as "the game in motion".
  Screenshot committed as `slur.png`, wired into a new **root `README.md`** (repo front door).

**Docs deliberately NOT touched:** GDD/TDD/ADD/AUDIO — nothing in the *design* changed; these are polish
fixes tracked as issues, not design decisions. ADD's palette/environment OQs remain the design authority.

**Still remaining in S6** (unchanged by this pass): `armour` stun-multiplier · pick-UI identity stats ·
integrate `environment.tsx` into the in-game net canvas · hull-colour + `COLOR_COUNT` 8→12 · trails. The
human feel-gate (#11) is still pending — procgen/audio/landing not yet *played*.
