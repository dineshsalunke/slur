# Phase note — 2026-08-06 — Project setup (Ideate → Reconcile)

Working title: **SLUR**. Casual LAN party ship-racer (office toy). Single session, full arc traversed:
ideate/brainstorm → research → design docs → align → scaffold → reconcile.

## Planned
- pnpm monorepo (`apps/` + `packages/`). Stack: Colyseus + React Router + R3F + koota.
- Research all stack idioms into `conventions/`; author GDD/TDD/ADD/AUDIO design docs.
- Scaffold a "minimal bootable" skeleton.

## Actual — decisions locked
- **Design:** constrained-flight movement (player throttle/brake, analog strafe, expressive jump, boost overdrive);
  third-person chase cam (stable-on-stun, comfort sliders); 3 ship classes v1 (Fighter/Freighter/Interceptor,
  data-driven, hitbox scales with armour); two modes (**Race** v1 / Survival endless w/ chase-wall);
  north star = **casual, mess-with-friends > fairness**; art = TRON/Star-Trek neon + LCARS HUD; audio = synthwave.
- **Stack (verified via npm 2026-08-06):** React Router **8** *framework mode SPA* (`ssr:false`); R3F 9 + drei +
  postprocessing (**three pinned `0.185.x`** — postprocessing peer `<0.186`); **koota** ECS; Colyseus **0.17**
  (server `@colyseus/core` 0.17.47; client `@colyseus/sdk` 0.17.43); pnpm monorepo, `@slur/*`, `shared`
  tsc-compiled ESM (schema decorator footgun); Biome 2.5.7 + ls-lint 2.3.1; TS 7.

## Actual — scaffold (SCOPE NARROWED mid-session)
User clarified **"strictly setup only, no game logic."** Delivered runnable **blank** skeletons:
- `@slur/shared` — placeholder export (schema/ships/sim written then **stripped** per scope).
- `@slur/server` — bare Colyseus boot on `:2567`, no rooms, imports shared to prove the workspace link.
- `@slur/client` — RR8 framework SPA, blank home page wired to `@slur/shared`; Tailwind + external fonts removed.
- **Verified:** `pnpm install/dev/build/typecheck/lint` all green; server boots; client serves 200; `pnpm dev` runs all three.

## Deviations from the conventions docs (now noted in those docs)
- Server transport = `@colyseus/core` + `@colyseus/ws-transport` + `express` (NOT `colyseus` meta — pnpm blocks its git uWebSockets subdep).
- RR mode: initially mis-scoped as *data mode* (I conflated "SPA" with "not framework mode"); **corrected to framework SPA** — SPA is a rendering strategy, orthogonal to router mode.
- pnpm gates handled: `allowBuilds` for esbuild/msgpackr-extract; global `minimumReleaseAge` exclusions.

## Next (implement phase)
1. Shared `simulate()` + `@colyseus/schema` classes (this finally validates the decorator/wire footgun).
2. Install deferred deps (R3F/three/koota/@colyseus/sdk) when the client renders the game.
3. First movement prototype (constrained flight feel).
4. Resolve remaining OPEN QUESTIONS: confirm v1 = Race; fuel depth; ship stat values; server-host location; prediction depth.
