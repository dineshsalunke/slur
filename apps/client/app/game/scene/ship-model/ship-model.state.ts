import type { EngineGlow } from '../ship-materials';
import { TEX_SPAN_X } from '../track-texture';

export const hullTexSpan = { value: TEX_SPAN_X };

export const glow: EngineGlow = { idle: 0, cruise: 0 };
