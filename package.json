{
    "name": "slur",
    "private": true,
    "type": "module",
    "packageManager": "pnpm@11.12.0",
    "engines": {
        "node": ">=22"
    },
    "scripts": {
        "dev": "pnpm -r --parallel run dev",
        "build": "pnpm -r run build",
        "typecheck": "pnpm -r run typecheck",
        "lint": "biome check . && ls-lint",
        "format": "biome format --write ."
    },
    "devDependencies": {
        "@biomejs/biome": "2.5.7",
        "@ls-lint/ls-lint": "2.3.1",
        "typescript": "catalog:"
    }
}
