// @slur/shared — code consumed by BOTH the client and the server
// (schema classes, shared simulate(), constants, ship-class data).
//
// Intentionally a placeholder for now: this is project/monorepo setup only.
// Real content lands in the implement phase.
export const SHARED_PACKAGE = '@slur/shared';
